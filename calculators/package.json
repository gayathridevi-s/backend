{
  "name": "calculator",
  "version": "1.0.0",
  "main": "calculator.js",
  "scripts": {
    "build": "rm -rf dist && npx tsc",
    "start-server": "npm run build && node dist/calculator.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.1.6"
  },
  "devDependencies": {},
  "description": ""
}
